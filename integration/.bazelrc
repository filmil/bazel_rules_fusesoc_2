common --registry=https://raw.githubusercontent.com/filmil/bazel-registry/main
common --registry=https://bcr.bazel.build

try-import %workspace%/user.bazelrc

# Nothing is taken from the machine the build runs on. A missing hermetic C
# toolchain then fails the build, instead of Bazel quietly using whatever
# compiler it finds.
build --action_env=BAZEL_DO_NOT_DETECT_CPP_TOOLCHAIN=1
build --repo_env=BAZEL_DO_NOT_DETECT_CPP_TOOLCHAIN=1

# rules_python starts a py_binary through a Python stage-1 script by default,
# "#!/usr/bin/env python3", which is whatever python3 the machine has. The
# script bootstrap starts from bash and uses the toolchain interpreter.
common --@rules_python//python/config_settings:bootstrap_impl=script
