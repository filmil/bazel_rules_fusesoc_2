name: Test
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: "43 7 * * 0"
concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      # Checkout first: the bazelisk cache is keyed on .bazelversion, which
      # has to exist when this runs.
      - name: Setup bazel
        uses: bazel-contrib/setup-bazel@0.19.0
        with:
          bazelisk-cache: true
          disk-cache: ${{ github.workflow }}
          repository-cache: true
      - name: Build
        # This is a test.
        run: "bazel build //..."
      - name: Test
        # This is a test.
        run: "bazel test //..."
      - name: Integration test
        run: "cd integration && bazel build @fusesoc_cores//... && bazel build //... && cat bazel-bin/fusesoc.txt"

